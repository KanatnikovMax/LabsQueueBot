namespace LabsQueueBot.Web.Commands.Implements;

public class BanCommandExecutor // TODO /ban
{
    // банить может как староста, так и админ
    // забанить старосту может только админ(или просто revoke)
    // админы могут банить во всех группах, старосты только в своей
    // бан подразумевает под собой черный список с автоматическим выходом из него
}